# Week 4

This week is about HTML and CSS layout concepts and practicing page structure, styling, and responsive design.
The assignments cover basic HTML elements and more advanced layouts using flexbox and grid.

## Contents

- `EX_1/`: Basic HTML elements and nested structure examples.
- `EX_2/`: Layout practice including flexbox, grid, forms, positioning, and navigation.

## What I did this week

- Built HTML examples with headings, paragraphs, lists, tables, and nested sections in `EX_1/`.
- Practiced layout techniques in `EX_2/`, using flexbox in `flex/`, grid in `grid/`, and form layouts in `form/`.
- Created a styled ID card example in `id/IDcARD.HTML` and a table layout in `table/index.html`.
- Built anchor navigation examples in `Anchor_assignment_1/` to move between city pages.
- Explored page composition with `pageusingFlex/`, `positions/`, and `front_assessgnment/flex/`.
- Used CSS styling to control layout, spacing, alignment, and responsive behavior.

## Code highlights

- `EX_2/flex/index.html`: flexbox layout examples for row and column placement.
- `EX_2/grid/index.html`: grid-based layout with multiple content sections.
- `EX_2/form/index.html`: form controls, labels, and input styling.
- `EX_2/table/index.html`: table structure with headers and rows.
- `EX_2/Anchor_assignment_1/`: separate linked pages for city navigation.

## Skills practiced

- Writing structured HTML with semantic tags
- Styling layout using CSS flexbox and grid
- Creating forms and input validation styles
- Organizing page sections and anchor navigation
- Using CSS for visual design and content layout
